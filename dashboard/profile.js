// ── Profile Page ──

// Save profile (stub)
function saveProfile() {
  alert('Profile saved successfully.');
}

// Discard changes — reload page to reset form values
function discardChanges() {
  if (confirm('Discard all unsaved changes?')) {
    location.reload();
  }
}

// Edit photo (stub)
function editPhoto() {
  alert('Photo upload coming soon.');
}